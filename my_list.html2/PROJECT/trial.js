/*document.addEventListener(`DOMContentLoaded`,function(){createCard("Cars","enter your car here.",)});
function createCard(title,body,imageUrl){
    const container = document.getElementById('card-container');
    const card = document.createElement('div');
    card.className = 'card';
const img = document.createElement('img');
img.src = imageUrl;
card.appendChild(img);

const CardTitle = document.createElement('h2');
CardTitle.textContent = title;
card.appendChild(CardTitle);

const cardBody = document.createElement('p');
cardBody.textContent = body;
card.appendChild(cardBody);

container.appendChild(card);
}


document.addEventListener(`DOMContentLoaded`,function(){createCard("Bikes","enter your bike here.",)});
function createCard(title,body,imageUrl){
    const container = document.getElementById('card-container');
    const card = document.createElement('div');
    card.className = 'card';
const img = document.createElement('img');
img.src = imageUrl;
card.appendChild(img);

const CardTitle = document.createElement('h2');
CardTitle.textContent = title;
card.appendChild(CardTitle);

const cardBody = document.createElement('p');
cardBody.textContent = body;
card.appendChild(cardBody);

container.appendChild(card);
}



document.addEventListener(`DOMContentLoaded`,function(){createCard("Others motors","enter your motors here.",)});
function createCard(title,body,imageUrl){
    const container = document.getElementById('card-container');
    const card = document.createElement('div');
    card.className = 'card';
const img = document.createElement('img');
img.src = imageUrl;
card.appendChild(img);

const CardTitle = document.createElement('h2');
CardTitle.textContent = title;
card.appendChild(CardTitle);

const cardBody = document.createElement('p');
cardBody.textContent = body;
card.appendChild(cardBody);

container.appendChild(card);
}

/*document.addEventListener('DOMContentLoaded', function() {
    const container = document.getElementById('card-container');
    createCard(container, "cars", "This is a card body 1.", "image-url-1.jpg");
    createCard(container, "bikes", "This is a card body 2.", "image-url-2.jpg");
    createCard(container, "other motors", "This is a card body 3.", "image-url-3.jpg");
});

function createCard(container, title, body, imageUrl) {
    const cardWrapper = document.createElement('div');
    cardWrapper.className = 'card-wrapper';

    const card = document.createElement('div');
    card.className = 'card';
    const img = document.createElement(img);
     img.src = imageUrl;
card.appendChild(img);

const cardTitle = document.createElement('h2')
cardTitle.textContent = title;
card.appendChild(cardTitle);

const cardBody = document.createElement('p');
cardBody.textContent = body;
card.appendChild(cardBody);

container.appendChild(card);
   

    cardWrapper.appendChild(card);
    container.appendChild(cardWrapper);
}
*/
